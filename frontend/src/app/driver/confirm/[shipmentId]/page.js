export default async function DeliveryConfirmationPage({ params }) {
  const { shipmentId } = await params;

  return (
    <main>
      <h1>Delivery Confirmation</h1>
      <p>TODO: implement</p>
    </main>
  );
}
